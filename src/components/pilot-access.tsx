"use client";import { FormEvent,useState } from "react";import { useRouter } from "next/navigation";
export function PilotAccess(){const router=useRouter();const[code,setCode]=useState("");const[error,setError]=useState<string|null>(null);const[busy,setBusy]=useState(false);async function submit(event:FormEvent){event.preventDefault();setBusy(true);setError(null);try{const response=await fetch("/api/pilot/access",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({code})});if(!response.ok)throw new Error("Access code not accepted.");setCode("");router.refresh();}catch(e){setError(e instanceof Error?e.message:"Unable to continue.");}finally{setBusy(false);}}return <main className="game-shell"><section className="game-card pilot-access"><p className="eyebrow">ERA PRINT PILOT</p><h1>Private testing access</h1><form onSubmit={submit}><label htmlFor="pilot-code">Access code</label><input id="pilot-code" type="password" autoComplete="current-password" value={code} onChange={e=>setCode(e.target.value)} required/><button className="primary-button" disabled={busy}>{busy?"Checking…":"Continue"}</button>{error&&<p className="game-error" role="alert">{error}</p>}</form></section></main>}
